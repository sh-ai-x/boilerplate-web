.claude/
logs/
tools/

# >>> dev-kit >>>
# dev-kit runtime artifacts (auto-installed by `dev-kit:ci-setup`; issue #202).
# Marker `.dev-kit/ci-config.json` is intentionally tracked so that
# `dev-kit:ci-setup --force` can detect drift. The paths below are
# runtime state — write-only by dev-kit, regenerated on each run —
# and must never be committed.
.dev-kit/.cost-gate/
.dev-kit/.eval-cache/
.dev-kit/logs/
.dev-kit/babysit.log
logs/
# <<< dev-kit <<<
__pycache__/

.tmp-tests

# Build artifacts (esbuild bundle output from scripts/build-cli.js).
dist/

# Installed dependencies — both pnpm-lock.yaml and package-lock.json are
# tracked at root. CI uses pnpm; local dev uses pnpm or npm.
node_modules/

