.claude/
logs/
tools/

# >>> dev-kit >>>
# dev-kit runtime artifacts (auto-installed by `dev-kit:ci-setup`; issue #202).
# Marker `.dev-kit/ci-config.json` is intentionally tracked so that
# `dev-kit:ci-setup --force` can detect drift. The paths below are
# runtime state — write-only by dev-kit, regenerated on each run —
# and must never be committed.
.dev-kit/.cost-gate/
.dev-kit/.eval-cache/
.dev-kit/logs/
.dev-kit/babysit.log
logs/
# <<< dev-kit <<<
__pycache__/

.tmp-tests

# Build artifact for the CLI. Regenerated by `npm run build:cli`.
# See scripts/build-cli.js.
dist/

# Installed dependencies — only package-lock.json is tracked. CI installs
# via `npm ci`; local dev runs `npm install`.
node_modules/
