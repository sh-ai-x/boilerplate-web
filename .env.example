# CI review provider — read by ci-doctor (process env → .env → .env.example chain)
# and by the /dev-kit:review + /dev-kit:security workflows via
# ${{ vars.CI_REVIEW_PROVIDER }} (set per-repo via `gh variable set`).
# Allowed values: minimax | anthropic
CI_REVIEW_PROVIDER=minimax
