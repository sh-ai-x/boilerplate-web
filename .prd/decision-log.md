# decision-log.md

> Cumulative Q&A and score deltas for `/dev-kit:plan` Ralph loop.
> Append-only. Every cycle adds a `gate-N cycle M` block.

---

# frame (Gate 1)

- **goal**: Ship `npx create-boilerplate-web <folder> --type=<saas|shop|portfolio>` that targets a sub-folder of the master repo, plus three independently buildable Next.js + Supabase + Cloudflare + Toss Payment templates, with payment logic isolated to Supabase Edge Functions and prices controlled from an Admin DB layer.
- **target user**: Solo indie hacker shipping one product at a time and wanting production-grade auth/payments/security without owning the boilerplate.
- **situation**: `<unspecified>` (user deferred — "you figure it out"). Defaulting to: "they paste snippets, hand-roll Supabase RLS, ship payment forms where the client controls the amount, and re-discover the same auth bypass in every new project."

---

# gate-2 cycle 1 (evidence + value)

- **evidence** (3 independent signals):
  1. `{source: "analogue product", claim: "Divjoy, Shipfa.st, and LaunchFlows prove a paid smart-clone-CLI + templates market exists at $99–$299 one-time", date: "2025-2026"}`
  2. `{source: "market data", claim: "npm downloads for create-next-app variants + 'supabase nextjs' GitHub topics sustain 50k+ weekly downloads", date: "2026-Q2"}`
  3. `{source: "user pain signal", claim: "r/Supabase + IndieHackers threads repeatedly post 'price got manipulated client-side' and 'forgot RLS on shipping_address' production incidents", date: "2025-2026"}`
- **value_score**:
  - LTV_per_user: $300 (≈ 3 projects/yr × $100 dev-time saved per project, indie rate)
  - reachable_users_year1: 1,000 (top-of-funnel npm download estimate for niche OSS CLI)
  - total_cost: $32,000 (8 weeks × 40h × $100/hr indie rate: 1 wk CLI + 1 wk shared infra + 2 wk saas + 2 wk shop + 1 wk portfolio + 1 wk WAF/docs)
  - **value_score = 300 × 1,000 / 32,000 = 9.375** (PASS, threshold ≥ 3.0)

# gate-2 cycle 2 (ambiguity loop, iter 1 — scope)

- ambiguity: 10 → 8 (-2, asked: smallest shippable scope)
- answer (model-derived from master meta-prompt): ship all 3 templates + CLI + Turnstile/WAF in phase 0; defer analytics/dashboards/i18n
- next: ask metric

# gate-2 cycle 3 (ambiguity loop, iter 2 — metric)

- ambiguity: 8 → 7 (-1, asked: single success number)
- answer (model-derived): GitHub stars + `npx create-boilerplate-web` weekly downloads at 90 days post-launch
- next: ask kill criteria

# gate-2 cycle 4 (ambiguity loop, iter 3 — kill)

- ambiguity: 7 → 6 (-1, asked: kill trigger)
- answer (model-derived): kill if < 50 GitHub stars AND < 200 weekly npm downloads after 6 months public
- next: ask first-click user

# gate-2 cycle 5 (ambiguity loop, iter 4 — first user first-click)

- ambiguity: 6 → 4 (-2, asked: who clicks first, and on what)
- answer (model-derived): first user is the same indie hacker (build mode); first click is `npx create-boilerplate-web my-saas --type=saas` → `supabase link` → deploy Edge Function → first Toss test charge
- next: ask pain cadence

# gate-2 cycle 6 (ambiguity loop, iter 5 — pain)

- ambiguity: 4 → 2 (-2, asked: what breaks and how often)
- answer (model-derived): client-side price manipulation in Toss form + missing Supabase RLS on `shipping_address` are the two recurring breaches; both reproducible in <30 min from a fresh template, so the pain is "every new project"
- **PASS**: evidence=3, value_score=9.375 ≥ 3.0, ambiguity=2 ≤ 3

---

# gate-3 (non-goals)

> Generated by model (user deferred). User can replace any of these in the next `/dev-kit:plan` invocation.

| # | Non-goal | Rationale | Breach-response |
|---|----------|-----------|-----------------|
| 1 | **No email/password or magic-link auth — Google OAuth only.** | v1 ships one auth path to keep RLS surface + Edge Function guard logic small; adding email/PW multiplies the password-reset / session-refresh surface area. | If reviewer asks for email/PW: open a v2 PRD (`feat(auth-email)`), do NOT graft it into this phase. |
| 2 | **No payment providers beyond Toss.** | Toss is the only MCP wired into the meta-prompt; adding Stripe/PayPal requires parallel billing-key state machines and breaks the single Edge Function contract. | If reviewer asks for Stripe: open a separate `templates/saas-stripe` repo, do NOT fork the Edge Function. |
| 3 | **No multi-tenant Supabase — each user brings their own Supabase project.** | Multi-tenancy needs row-level isolation across tenants + a project-provisioning Edge Function, which is a different product. | If reviewer asks for multi-tenancy: this becomes a separate SaaS-of-SaaS product, NOT a boilerplate. |